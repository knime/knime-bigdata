<html>
<body style='width: 350px'>
<p>
<strong>Estimated total cluster resources:</strong><br> ${total.memory.min}-${total.memory.max} ${memory.unit.total} of memory and 
${total.cores.min}-${total.cores.max} cores.
</p>
<p></p>
<strong>Estimated per-container resources:</strong>
<ul style='margin-left: 20px;'>
<li>
 one Spark driver with ${driver.memory} ${memory.unit.containers} of memory and ${driver.cores} core(s)
</li>
<li>
 ${executors.min}-${executors.max} Spark executors, each with ${executor.memory} ${memory.unit.containers} of memory and ${executor.cores} core(s)
 </li>
</ul>
</body>
</html>
