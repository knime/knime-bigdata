knime {
  greetings = ".... Copyright 2015 KNIME"
  goodbye = "... completed. Bye!"
    
  //logger file location
  logFile = "/var/log/knime/main.log"
  
  active {
     what_ever = false
  }
}

spark {
  greetings = "... Copyright 2015 KNIME / SPARK"

  //TODO - can we determine this user from KNIME (do we want to determine the user name from KNIME?)
  //this should probably be part of a config node
  userName = "spark"
  
  //ip and port of Spark Job Server
  jobServer = "sandbox.hortonworks.com"
  //jobServer = "134.119.17.134"
  jobServerPort = 8090

  //name of Spark context. Should be the per Spark job server.
  contextName = "knimeSparkContext"
  
  //number of CPU cores
  numCPUCores = 2
  //memory per executor node
  memPerNode = "512m"
}
